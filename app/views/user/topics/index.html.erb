<%# TODO: Refactor to use presenter pattern %>
<% @facade.topics.each do |topic|%>
  <%=  topic.title %>
<% end %>

<%= form_for @facade.new_topic do |f| %>
  <%= f.text_field :title, placeholder: 'Health' %>
  <%= f.submit 'Submit' %>
<% end %>

